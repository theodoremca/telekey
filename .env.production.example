# Overlay on `.env` when TELEKEY_STAGE=production in `.env` (or the shell).
# No Stripe. No TeleKey OpenAI key.

TELEKEY_API_BASE=https://us-east1-telekey-app.cloudfunctions.net/api
TELEKEY_SITE_URL=https://telekey.vercel.app
