{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for TeleKey windows",
  "windows": ["main", "overlay", "settings", "history"],
  "permissions": [
    "core:default",
    "global-shortcut:default"
  ]
}
