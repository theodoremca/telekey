{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for TeleKey windows",
  "windows": ["main", "overlay", "settings", "history", "setup"],
  "permissions": [
    "core:default",
    "global-shortcut:default",
    "deep-link:default"
  ]
}
