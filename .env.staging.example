# Overlay on `.env` when TELEKEY_STAGE=staging (the daily default in `.env`).
# No Stripe. No TeleKey OpenAI key.

TELEKEY_API_BASE=https://us-east1-telekey-app.cloudfunctions.net/staging
TELEKEY_SITE_URL=https://telekey-staging.vercel.app
